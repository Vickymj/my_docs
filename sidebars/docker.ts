import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const dockerSidebar: SidebarsConfig['Docker'] = [
  {
    type: 'category',
    label: 'Docker',
    items: ['docker/docker_intro'],
  },
];

export default dockerSidebar;