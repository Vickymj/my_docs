---
id: installation_basic_setup
title: FastAPI - Installation & Setup
---


## Installation
- python lastest version
- python -m venv .venv
-  .venv\scripts\activate
- uv add fastapi[all]/ pip install 
- uv add scalar_fastapi
- pip install ptpython # for interactive shell

## Extension
- python
- ruff
- mypy
- error lens


## API EndPonint

```py tilte="main.py"

from fastapi import FastAPI

app = FastAPI()


@app.get("/shipment")
def get_shipment():
    # return dict(content="wooden table", status= "in transit" )
    return {
        "content": "wooden table",
        "status": "in transit"
    }

```
optional (API Documentation)
```py title="main.py"
from fastapi import FastAPI
from scalar_fastapi import get_scalar_api_reference

app = FastAPI()


@app.get("/shipment")
def get_shipment():
    return {
        "content": "wooden table",
        "status": "in transit"
    }


# Scalar API Documentation
@app.get("/scalar", include_in_schema=False)
def get_scalar_docs():
    return get_scalar_api_reference(
        openapi_url=app.openapi_url,
        title="Scalar API",
    )
```


## Run command
```cmd
fastapi dev
```







